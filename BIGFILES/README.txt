Put the big raw data files here. Then use the following command:

mountainprocess set-big-file-search-path /full/path/to/BIGFILES

Note that it does not matter how the big raw data files are named since they are searched by checksum and size.
